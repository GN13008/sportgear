<div class="content">
  <h2>Log in</h2>
  <h3>No need to login YET</h3>
  <p>We are working of a new version of SportGear where login could be usefull but for the moment the function is not available and is present only to test function</p>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in" %>
    </div>
  <% end %>

  <%# <%= render "devise/shared/links" %> <%#%>
</div>
